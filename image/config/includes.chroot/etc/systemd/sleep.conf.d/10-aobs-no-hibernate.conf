# 01-boot-layer.md §4. Hibernation writes RAM — and therefore a loaded seed — to disk.
# `nohibernate` is already on the kernel command line (§6); this is the userspace half.
[Sleep]
AllowHibernation=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
